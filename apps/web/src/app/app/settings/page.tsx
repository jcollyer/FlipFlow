import { SettingsView } from '@/features/settings/SettingsView';

/**
 * /app/settings — user profile settings (currently just display name).
 */
export default function SettingsPage() {
  return <SettingsView />;
}
