import { FoldersDashboard } from '@/features/folders/FoldersDashboard';

/**
 * /app/folders — list all folders. Sister page to /app/all-categories.
 */
export default function FoldersPage() {
  return <FoldersDashboard />;
}
